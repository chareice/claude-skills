# Health

Quick health check for the target environment.

1. Hit the environment's health endpoint (from runbook) and report response status + body
2. If health check fails, suggest running `diagnose` for deeper investigation
